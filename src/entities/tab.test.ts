import { makeTab } from './tab';

describe('src/entities/tab.ts', () => {
  describe('makeTab', () => {
    it('should be a function', () => {
      expect(makeTab).toBeInstanceOf(Function);
    });
  })
});